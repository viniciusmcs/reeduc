# Django settings
DJANGO_SECRET_KEY=your-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost,10.0.125.4
USE_SQLITE=True

# Optional PostgreSQL settings (used only if USE_SQLITE=False)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=reeducdb
DB_USER=sejus
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5432

# Internal network over HTTP
CSRF_TRUSTED_ORIGINS=http://10.0.125.4

# Default admin (dev only)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=change-me
DEFAULT_ADMIN_EMAIL=admin@example.com
